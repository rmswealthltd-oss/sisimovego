{
  "name": "sisimovego-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",

  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "prepare": "husky install"
  },

  "workspaces": [
    "apps/*"
  ],

  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.6.0",
    "turbo": "^2.0.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.1.0",
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.0",
    "prisma": "^5.22.0"
  },

  "dependencies": {
    "@prisma/client": "^5.22.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "helmet": "^6.0.1",
    "morgan": "^1.10.1",
    "express-rate-limit": "^8.2.1"
  },

  "lint-staged": {
    "*.{ts,tsx,js,json,md}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
